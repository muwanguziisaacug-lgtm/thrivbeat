"use client";
import { useState } from "react";
export default function CheckoutButton(){const [busy,setBusy]=useState(false),[error,setError]=useState("");async function checkout(){setBusy(true);setError("");try{const response=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({plan:"STANDARD",period:"MONTHLY"})});const data=await response.json();if(response.status===401){location.href="/login?next=/pricing";return}if(!response.ok||!data.url)throw new Error(data.error||"Checkout is unavailable");location.href=data.url}catch(e){setError(e.message);setBusy(false)}}return <div><button onClick={checkout} disabled={busy} className="w-full rounded-full bg-rose-800 px-6 py-3.5 font-bold text-white">{busy?"Opening secure checkout…":"Choose monthly access"}</button>{error&&<p role="alert" className="mt-3 text-sm text-red-700">{error}</p>}</div>}
